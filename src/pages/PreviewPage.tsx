import { Maximize2,Smartphone } from 'lucide-react';import { useProjectStore } from '../project/store';import { useEditorStore } from '../app/editorStore';import { MiniAppPreview } from '../preview/MiniAppPreview';import { DeviceFrame } from '../preview/DeviceFrame';
export function PreviewPage(){const {project,resetModuleConfig}=useProjectStore();const {previewSource,setPreviewSource,device,setDevice}=useEditorStore();const shown=previewSource==='published'&&project.published?project.published.snapshot:project;return <div className="page preview-page"><div className="page-title"><div><span className="eyebrow">Client renderer</span><h1>Preview</h1></div><div className="preview-controls"><div className="segmented"><button className={previewSource==='draft'?'active':''} onClick={()=>setPreviewSource('draft')}>Draft</button><button className={previewSource==='published'?'active':''} disabled={!project.published} onClick={()=>setPreviewSource('published')}>Published</button></div><button className="icon-button" onClick={()=>setDevice(device==='phone'?'full':'phone')} aria-label="Сменить устройство">{device==='phone'?<Maximize2/>:<Smartphone/>}</button></div></div>{previewSource==='published'&&!project.published&&<p className="notice">Сначала опубликуйте проект.</p>}<DeviceFrame mode={device}><MiniAppPreview project={shown} onResetModule={resetModuleConfig}/></DeviceFrame></div>}
