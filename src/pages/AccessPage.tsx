import { useProjectStore } from '../project/store';import { TelegramSubscriptionSettings } from '../guards/telegram-subscription/TelegramSubscriptionSettings';import { TelegramSubscriptionPreview } from '../guards/telegram-subscription/TelegramSubscriptionPreview';export function AccessPage(){const {project,mutate}=useProjectStore(),guard=project.guards.find(g=>g.type==='telegram_subscription');if(!guard)return <div className="page"><h1>Доступ</h1><p>Guard не входит в этот blueprint.</p></div>;return <div className="page"><div className="page-title"><div><span className="eyebrow">Guards</span><h1>Доступ</h1></div></div><div className="access-layout"><TelegramSubscriptionSettings guard={guard} onChange={next=>mutate(p=>({...p,guards:p.guards.map(g=>g.id===next.id?next:g)}))}/><TelegramSubscriptionPreview config={guard.config}/></div></div>}
