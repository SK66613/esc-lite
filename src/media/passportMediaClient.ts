import { z } from 'zod';
import { PassportMediaIdSchema } from '../modules/loyalty-passport/schema';
import { getTelegramInitData } from '../telegram/getTelegramInitData';
const ResponseSchema=z.object({mediaId:PassportMediaIdSchema,contentType:z.literal('image/jpeg'),bytes:z.number().int().positive().max(3*1024*1024),width:z.number().int().positive().max(4096),height:z.number().int().positive().max(4096)}).strict().refine(value=>value.width*value.height<=16_000_000);
export interface PassportMediaClientDeps { fetch:typeof fetch; getInitData:()=>string }
const messages:Record<string,string>={MEDIA_FILE_TOO_LARGE:'Файл слишком большой.',MEDIA_PROJECT_QUOTA:'В этом проекте достигнут лимит загруженных фото.',MEDIA_USER_QUOTA:'Достигнут лимит загруженных фото.',MEDIA_RATE_LIMIT:'Слишком много загрузок. Попробуйте через минуту.',MEDIA_INVALID_TYPE:'Неподдерживаемый формат изображения.',MEDIA_INVALID_JPEG:'Не удалось обработать изображение.',MEDIA_NOT_CONFIGURED:'Хранилище временно недоступно. Попробуйте позже.',MEDIA_STORAGE_ERROR:'Хранилище временно недоступно. Попробуйте позже.'};
export async function uploadPassportCover({projectId,file}:{projectId:string;file:File},deps:PassportMediaClientDeps={fetch,getInitData:getTelegramInitData}){const initData=deps.getInitData();if(!initData)throw new Error('Сессия Telegram устарела. Переоткройте приложение.');const body=new FormData();body.append('file',file);body.append('projectId',projectId);let response:Response;try{response=await deps.fetch('/api/media/passport-covers',{method:'POST',headers:{'X-Telegram-Init-Data':initData},body});}catch{throw new Error(messages.MEDIA_STORAGE_ERROR);}if(!response.ok){const value=await response.json().catch(()=>({})) as {code?:string};if(response.status===401)throw new Error('Сессия Telegram устарела. Переоткройте приложение.');throw new Error(value.code&&messages[value.code]||messages.MEDIA_STORAGE_ERROR);}const parsed=ResponseSchema.safeParse(await response.json());if(!parsed.success)throw new Error('Хранилище вернуло некорректный ответ.');return parsed.data;}
