import {z} from 'zod';import {getTelegramInitData} from '../telegram/getTelegramInitData';
const ResponseSchema=z.object({mediaId:z.string().max(100),contentType:z.literal('image/jpeg'),bytes:z.number().int().positive().max(3*1024*1024),width:z.number().int().positive().max(4096),height:z.number().int().positive().max(4096)}).strict();
export async function uploadPassportCover({projectId,file}:{projectId:string;file:File}){const initData=getTelegramInitData();if(!initData)throw new Error('Сессия Telegram устарела. Переоткройте приложение.');const body=new FormData();body.append('file',file);body.append('projectId',projectId);let response:Response;try{response=await fetch('/api/media/passport-covers',{method:'POST',headers:{'X-Telegram-Init-Data':initData},body});}catch{throw new Error('Хранилище временно недоступно. Попробуйте позже.');}if(!response.ok){const value=await response.json().catch(()=>({})) as {code?:string};if(response.status===401)throw new Error('Сессия Telegram устарела. Переоткройте приложение.');if(value.code==='MEDIA_FILE_TOO_LARGE')throw new Error('Файл слишком большой.');throw new Error('Хранилище временно недоступно. Попробуйте позже.');}const parsed=ResponseSchema.safeParse(await response.json());if(!parsed.success)throw new Error('Хранилище вернуло некорректный ответ.');return parsed.data;}
