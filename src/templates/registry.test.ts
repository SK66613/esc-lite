import { describe,expect,it } from 'vitest';import { templateRegistry } from './registry';
describe('template blueprints',()=>{it('coffee house creates modules, tool and guard',()=>{const project=templateRegistry.coffee_house.createProject();expect(project.modules.map(m=>m.type)).toEqual(['loyalty_passport','offers_placeholder']);expect(project.tools.map(t=>t.type)).toEqual(['qr_sales']);expect(project.guards.map(g=>g.type)).toEqual(['telegram_subscription'])});it('blueprints have different structures',()=>expect(templateRegistry.store.createProject().modules.map(m=>m.type)).toEqual(['offers_placeholder']))});
