import { templateRegistry,type TemplateId } from './registry';import { useProjectStore } from '../project/store';import { useEditorStore } from '../app/editorStore';import { Sheet } from '../ui/Sheet';
export function TemplateSheet(){const replace=useProjectStore(s=>s.replace),setSheet=useEditorStore(s=>s.setSheet);return <Sheet title="Создать из шаблона" onClose={()=>setSheet(undefined)}><p className="sheet-description">Шаблон заменит текущий локальный проект реальным blueprint. Опубликуйте изменения заранее, если хотите сохранить snapshot.</p><div className="template-grid">{Object.values(templateRegistry).map(template=><button key={template.id} onClick={()=>{if(confirm(`Заменить проект шаблоном «${template.title}»?`)){replace(templateRegistry[template.id as TemplateId].createProject());setSheet(undefined)}}}><span className={`template-art ${template.id}`}/><span><strong>{template.title}</strong><small>{template.description}</small></span></button>)}</div></Sheet>}
