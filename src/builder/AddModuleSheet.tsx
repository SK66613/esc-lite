import { Plus } from 'lucide-react';import { moduleRegistry,type ModuleType } from '../modules/registry';import type { ModuleInstance } from '../project/schema';import { Sheet } from '../ui/Sheet';
export function AddModuleSheet({existing,onAdd,onClose}:{existing:ModuleInstance[];onAdd:(type:ModuleType)=>void;onClose:()=>void}){return <Sheet title="Добавить блок" onClose={onClose}><div className="sheet-list">{Object.values(moduleRegistry).map(def=>{const Icon=def.icon,exists=existing.some(m=>m.type===def.type);return <button key={def.type} disabled={exists} onClick={()=>{onAdd(def.type as ModuleType);onClose()}}><Icon/><span><strong>{def.title}</strong><small>{exists?'Уже добавлен':def.description}</small></span><Plus/></button>})}</div></Sheet>}
