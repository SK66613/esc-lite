import { OffersConfigSchema, type OffersConfig } from './schema';
export function OffersInspector({config,onChange}:{config:unknown;onChange:(c:OffersConfig)=>void}){const c=OffersConfigSchema.parse(config);return <div className="inspector-form"><label>Название<input value={c.title} onChange={e=>onChange({...c,title:e.target.value})}/></label><label>Текст пустого состояния<textarea value={c.emptyText} onChange={e=>onChange({...c,emptyText:e.target.value})}/></label><p className="hint">Полноценные Offers появятся в следующей итерации. Сейчас модуль подтверждает расширяемость registry.</p></div>}
