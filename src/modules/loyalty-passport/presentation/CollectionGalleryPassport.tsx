import { Gift,LockKeyhole } from 'lucide-react'; import type { PassportRendererProps } from './types'; import { Header,Progress,QRButton,Reward } from './Shared';
export function CollectionGalleryPassport(props:PassportRendererProps){const {viewModel:v,presentation:p}=props;return <article className="passport passport-collection"><Header {...props}/>{v.showProgress&&<><div className={`passport-gallery passport-gallery--${p.imageAspect}`} style={{gridTemplateColumns:`repeat(${p.columns},minmax(0,1fr))`}}>{v.stamps.map(s=><div key={s.index} className={s.completed?'is-complete':''}>{s.completed?<Gift/>:<LockKeyhole/>}<small>Объект {s.index+1}</small><strong>{s.completed?'Открыт':'Скрыт'}</strong></div>)}</div><Progress {...props}/></>}<Reward viewModel={v}/><QRButton/></article>}
