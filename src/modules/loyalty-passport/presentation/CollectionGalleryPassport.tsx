import {PassportStampIcon} from '../content/iconRegistry'; import type { PassportRendererProps } from './types'; import { Cover,Header,Progress,QRButton,Reward } from './Shared';
export function CollectionGalleryPassport(props:PassportRendererProps){const {viewModel:v,presentation:p}=props;return <article className="passport passport-collection"><Header {...props}/>{v.showProgress&&<><div className={`passport-gallery passport-gallery--${p.imageAspect}`} style={{gridTemplateColumns:`repeat(${p.columns},minmax(0,1fr))`}}>{v.stamps.map(s=><div key={s.index} className={s.completed?'is-complete':''}><div className="passport-gallery-cover"><Cover stamp={s} fallback/><span><PassportStampIcon iconKey={s.iconKey}/></span></div><small>{s.title}</small><strong>{s.completed?'Открыт':'Закрыт'}</strong></div>)}</div><Progress {...props}/></>}<Reward viewModel={v}/><QRButton/></article>}
