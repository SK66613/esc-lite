import type { PassportRendererProps } from './types'; import { Header,Progress,QRButton,Reward } from './Shared';
export function JourneyPathPassport(props:PassportRendererProps){const {viewModel:v}=props;return <article className="passport passport-journey"><Header {...props}/>{v.showProgress&&<><div className="passport-path">{v.stamps.map(s=><div key={s.index} className={s.completed?'is-complete':''}><span>{s.completed?'✓':s.index+1}</span><small>{s.completed?'Пройдено':s.index===v.filled?'Сейчас':'Впереди'}</small></div>)}</div><Progress {...props}/></>}<Reward viewModel={v}/><QRButton/></article>}
