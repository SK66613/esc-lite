import {PassportStampIcon} from '../content/iconRegistry'; import type { PassportRendererProps } from './types'; import { Cover,Header,Progress,QRButton,Reward } from './Shared';
export function JourneyPathPassport(props:PassportRendererProps){const {viewModel:v}=props;return <article className="passport passport-journey"><Header {...props}/>{v.showProgress&&<><div className="passport-path">{v.stamps.map(s=><div key={s.index} className={s.completed?'is-complete':''}><span><PassportStampIcon iconKey={s.iconKey}/></span><Cover stamp={s} className="passport-path-cover"/><div><strong>{s.title}</strong><small>{s.completed?'Пройдено':s.index===v.filled?'Сейчас':'Впереди'}</small></div></div>)}</div><Progress {...props}/></>}<Reward viewModel={v}/><QRButton/></article>}
