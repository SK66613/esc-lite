import type { PassportRendererProps } from './types'; import { Header,Progress,QRButton,Reward } from './Shared';
export function ClassicGridPassport(props:PassportRendererProps){const {viewModel:v,presentation:p}=props;return <article className="passport passport-classic"><Header {...props}/><Progress {...props}/><div className="passport-grid" style={{gridTemplateColumns:`repeat(${p.columns},1fr)`}}>{v.stamps.map(s=><span key={s.index} className={`passport-stamp passport-stamp--${p.stampShape} ${s.completed?'is-complete':''}`} aria-label={s.title}>{s.completed?'✓':s.index+1}</span>)}</div><Reward viewModel={v}/><QRButton/></article>}
