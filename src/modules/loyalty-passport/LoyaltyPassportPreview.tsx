import type { LoyaltyPassportConfig } from './schema'; import { buildPassportViewModel } from './presentation/buildPassportViewModel'; import { normalizePassportPresentation } from './presentation/normalizePresentation'; import { passportPresentationById } from './presentation/registry'; import './passport.css';
export function LoyaltyPassportPreview({config}:{config:LoyaltyPassportConfig}){const presentation=normalizePassportPresentation(config.presentation);const viewModel=buildPassportViewModel(config);const Renderer=passportPresentationById[presentation.visualVariant].Renderer;return <Renderer viewModel={viewModel} presentation={presentation}/>}
