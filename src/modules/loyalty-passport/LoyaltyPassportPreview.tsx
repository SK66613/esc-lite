import { Gift, QrCode } from 'lucide-react';
import type { LoyaltyPassportConfig } from './schema';
export function LoyaltyPassportPreview({config:c}:{config:LoyaltyPassportConfig}){const filled=Math.min(c.goal,Math.floor(c.balance));return <article className="loyalty-preview"><div className="preview-welcome"><div><span className="eyebrow">Программа лояльности</span><h2>{c.name}</h2><p>{c.description}</p></div><div className="reward-art"><Gift/></div></div>{c.showProgress&&<><div className="progress-label"><span>{filled} из {c.goal}</span><strong>{c.reward}</strong></div><div className="stamps">{Array.from({length:c.goal},(_,i)=><span key={i} className={i<filled?'filled':''}>{i<filled?'✓':i+1}</span>)}</div></>}<button className="client-action"><QrCode/> Показать QR для начисления</button></article>}
