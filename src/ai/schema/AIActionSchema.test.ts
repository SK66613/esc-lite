import {describe,expect,it} from 'vitest';import {AIActionSchema} from './AIActionSchema';describe('AIActionSchema',()=>{it('accepts a valid action',()=>expect(AIActionSchema.safeParse({type:'set_metadata',payload:{name:'Coffee House'}}).success).toBe(true));it('rejects malformed actions',()=>expect(AIActionSchema.safeParse({type:'set_module_enabled',payload:{moduleType:'offers_placeholder',enabled:'yes'}}).success).toBe(false))});
