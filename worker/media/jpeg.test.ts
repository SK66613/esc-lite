import {describe,expect,it} from 'vitest';import {inspectJPEG} from './jpeg';
const jpeg=(w:number,h:number)=>new Uint8Array([0xff,0xd8,0xff,0xc0,0,17,8,h>>8,h&255,w>>8,w&255,3,1,0x11,0,2,0x11,0,3,0x11,0]);
describe('JPEG inspector',()=>{it('reads dimensions',()=>expect(inspectJPEG(jpeg(1200,900))).toEqual({width:1200,height:900}));it('rejects invalid magic',()=>expect(()=>inspectJPEG(new Uint8Array([1,2,3]))).toThrow('INVALID_MAGIC'));it('rejects truncation',()=>expect(()=>inspectJPEG(new Uint8Array([0xff,0xd8,0xff,0xc0,0,17]))).toThrow('TRUNCATED'));it('rejects zero dimensions',()=>expect(()=>inspectJPEG(jpeg(0,2))).toThrow('INVALID_DIMENSIONS'));it('rejects oversized dimensions',()=>expect(()=>inspectJPEG(jpeg(4097,2))).toThrow('DIMENSIONS_TOO_LARGE'));it('rejects pixel limit',()=>expect(()=>inspectJPEG(jpeg(4096,4096))).toThrow('PIXELS_TOO_LARGE'));});
