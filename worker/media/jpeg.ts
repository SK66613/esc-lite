export class JPEGError extends Error{}
export function inspectJPEG(bytes:Uint8Array){if(bytes.length<4||bytes[0]!==0xff||bytes[1]!==0xd8)throw new JPEGError('INVALID_MAGIC');let offset=2;while(offset<bytes.length){while(offset<bytes.length&&bytes[offset]===0xff)offset++;if(offset>=bytes.length)break;const marker=bytes[offset++];if(marker===0xd9||marker===0xda)break;if(marker===0x01||(marker>=0xd0&&marker<=0xd7))continue;if(offset+2>bytes.length)throw new JPEGError('TRUNCATED');const length=(bytes[offset]<<8)|bytes[offset+1];if(length<2||offset+length>bytes.length)throw new JPEGError('TRUNCATED');if((marker>=0xc0&&marker<=0xc3)||(marker>=0xc5&&marker<=0xc7)||(marker>=0xc9&&marker<=0xcb)||(marker>=0xcd&&marker<=0xcf)){if(length<7)throw new JPEGError('INVALID_DIMENSIONS');const height=(bytes[offset+3]<<8)|bytes[offset+4],width=(bytes[offset+5]<<8)|bytes[offset+6];if(!width||!height)throw new JPEGError('INVALID_DIMENSIONS');if(width>4096||height>4096)throw new JPEGError('DIMENSIONS_TOO_LARGE');if(width*height>16_000_000)throw new JPEGError('PIXELS_TOO_LARGE');return {width,height};}offset+=length;}throw new JPEGError('NO_DIMENSIONS');}
