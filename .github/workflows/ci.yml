name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      # Switch to npm ci when registry access allows committing package-lock.json.
      - run: npm install
      - run: npm run build
      - run: npm run test
